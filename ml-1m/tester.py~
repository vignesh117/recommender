from __future__import division
from __future__ import division
from read_movie_info import *

ratings_file = open('ratings.dat')
user = '5'
user_rating = ['4','5']
movies = []
movie = '1964'
for line in ratings_file.readlines():
    temp = line.split('::')
    info = (temp[0],temp[1],temp[2])
    movies.append(info)

for_user = [x for x in movies if x[0] == user and x[2] in user_rating]

for_user = [x[1] for x in movies if x[0] == user and x[2] in user_rating]
for_user
map(get_movie_name,for_user)
from read_movie_info import *
map(get_movie_name,for_user)
from read_movie_info import *
map(get_movie_name,for_user)
from read_movie_info import *
map(get_movie_name,for_user)
from collections imort defaultdict
from collections import defaultdict
a = defaultdict()
a{'1' :3}
a{'1',3}
a{'1':3}
a['1'] = 3
a.get('2')
a = {}
a['1'] = 3
a['2'
]
a = defaultdict(a)
from read_movie_info import *
map(get_movie_name,for_user)
%history

